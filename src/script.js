// funkcja wyświetlająca imię i nazwisko
const showName = () => {
    const user = document.getElementById("userName");
    user.textContent = "Paulina Cudak"; // wyświetlane imię i nazwisko
};
showName();